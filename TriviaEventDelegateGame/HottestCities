using System;
using System.Collections.Generic;

namespace List_Practise
{
    class Program
    {
        static void Main(string[] args)
        {
            double beijing = (64 + 44) / 2;
            double rome = (71 + 48) / 2;
            double berlin = (56 + 43) / 2;
            double london = (60 + 46) / 2;
            double tokyo = (67 + 52) / 2;
            double losAngeles = (75 + 56) / 2;
            double capeTown = (71 + 52) / 2;

            var destinations = new List<object> { "Beijing: {0}", "Rome: {1}", "Berlin: {2}", "London: {3}",
                "Tokyo: {4}", "Los Angeles: {5}", "Cape Town: {6}", beijing, rome, berlin, london, tokyo, losAngeles, capeTown};

            Console.WriteLine(destinations.Count);

            /*var hottestCities = GetHottestCities(destinations, 2);

            foreach(var name in hottestCities)
            {
                Console.WriteLine(name);
            }                
        }

        public static List<object> GetHottestCities(List<object> list, int count)
        {
            var cities = new List<object>();

            while (cities.Count < count)
            {
                var currentCity = GetHottestCity(list);
                cities.Add(currentCity);
                list.Remove(currentCity);
            }
            return cities;
        }

        public static object GetHottestCity(List<object> list)
        {
            object hottestCity = list[0];
            for(var i=0; i<list.Count; i ==)
            {

            */}

        }
    }
}
